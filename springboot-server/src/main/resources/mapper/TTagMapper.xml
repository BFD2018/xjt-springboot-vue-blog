<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.xjt.blog.mapper.TTagMapper">
	
	<select id="selectTagToBlogNum" resultType="int">
		select count(*) from t_blog_tags where tags_id=#{tag_id}
	</select>

</mapper>